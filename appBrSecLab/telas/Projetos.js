import { Text, SafeAreaView } from "react-native";

export default function Projetos() {
    return (
        <SafeAreaView>
            <Text>Projetos</Text>
            <Text>Em construção</Text>
        </SafeAreaView>
    );
}